# Dependencies

FastAPI dependency injection functions for core components.

::: chapkit.core.api.dependencies
    options:
      members:
        - get_database
        - get_session
        - get_scheduler
